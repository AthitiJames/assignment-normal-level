<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Table</title>
    <link rel="stylesheet" href="user-list-styles.css">
  </head>
  <body>
    <div class="table-container">
      <div class="table-header">
        <h2>Users Information</h2>
        <%= link_to 'New User', new_user_path, class: "new-user-btn" %>
      </div>
      <table class="user-table">
        <thead>
          <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Birthday</th>
            <th>Gender</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Subject</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= user.first_name %></td>
              <td><%= user.last_name %></td>
              <td><%= user.birthday %></td>
              <td><%= user.gender %></td>
              <td><%= user.email %></td>
              <td><%= user.phone %></td>
              <td><%= user.subject %></td>
              <td>
                <%= link_to 'Edit', edit_user_path(user) %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </body>
</html>